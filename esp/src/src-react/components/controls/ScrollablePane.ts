//  v8 ScrollablePane / Sticky shim
//
//  FluentUI v9 does not yet provide a like-for-like ScrollablePane /
//  Sticky pair. Until a replacement is wired up, consumers go through
//  this shim so the eventual migration touches a single file.

export { ScrollablePane, ScrollbarVisibility, Sticky, StickyPositionType } from "@fluentui/react";
