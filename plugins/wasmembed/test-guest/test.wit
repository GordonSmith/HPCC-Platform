/* 
interface iterator {
  record row {
    value: string,
    done: bool,
  }

  next: func() -> row
}
*/

package wasmembed:test

world test {
  //  import print: func(msg: string)

  export new-buffer: func(size: u32) -> u32
  export delete-buffer: func(buff: u32)

  export echo: func(msg: string) -> string
  export add: func(a: u32, b:u32) -> u32
  export add3: func(a: u32) -> u32
  export sub: func(a: u32, b:u32) -> u32
}
